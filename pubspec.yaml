name: fitness_app_x
description: Practice fitness app example.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0

  cupertino_icons: ^1.0.6
  pedantic: ^1.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
